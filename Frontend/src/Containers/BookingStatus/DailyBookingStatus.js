import React from "react";
import { useHistory } from "react-router-dom";

export default function DailyBookingStatus({ props }) {
  const history = useHistory();
  return <div className='container mx-auto'>สถานะการจองรายวัน</div>;
}
